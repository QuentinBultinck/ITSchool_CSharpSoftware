<UserControl x:Class="MyFriendOrganizer.UI.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyFriendOrganizer.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!-- TwoWay zal ook wegschrijven naar de MainViewModel als het hier veranderd -->
        <!-- DisplayMemberPath is de text die moet getoond worden dus DisplayMember = "Firstname LastName" in NavigationViewModel-->
        <ListView ItemsSource="{Binding Friends}"
                  SelectedItem="{Binding SelectedFriend, Mode=TwoWay}"
                  DisplayMemberPath="DisplayMember"/>
    </Grid>
</UserControl>
